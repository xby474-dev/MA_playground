# 贡献指南

欢迎数学勘误、教学反馈、可访问性和工程改进。反馈时附上实验分享链接、设备/浏览器、期望结果与实际结果。没有截图也可以通过参数复现。

## 数学内容

先写清研究点、定义域、原点补定义、候选映射、路径与量词。一般结论必须有推导或证明；测试与图像不能替代。新增反例需要一个完整证明，并写入 `docs/MATHEMATICS.md` 与对应测试。

只接受可以完整学习的实验，不增加“敬请期待”的空模块。每个实验至少包含一个真实困惑、可操作探索、解析解释与自测。

## 工程改动

运行 `npm run verify`；涉及界面时再运行 `npm run test:ui`。提交中说明是否使用标准 HTTP 模式，或受限环境的 standalone 注入模式；不能把后者写成线上 E2E 已通过。

不要添加外部 CDN、分析追踪、任意 `eval` 或未经验证的 URL HTML。新增 npm 依赖前说明它解决的问题与离线替代方案。新增 import 语法时注意单文件合并器只支持本地命名导入/导出；必要时采用正式打包器并同步测试。

保持小而可审核的提交。不得提交个人凭证、系统字体文件或用户隐私数据。
